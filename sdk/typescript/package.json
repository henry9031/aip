{
  "name": "@anthropic-aip/sdk",
  "version": "0.1.0",
  "description": "AIP (Agent Interchange Protocol) — SDK for agent-to-agent discovery and collaboration",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "src", "README.md", "LICENSE"],
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "test:watch": "vitest",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["aip", "agent", "protocol", "ai", "multi-agent", "discovery", "interoperability", "mcp"],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/henry9031/aip"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/node": "^25.3.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
